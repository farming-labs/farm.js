{
  "name": "farm.js",
  "private": true,
  "description": "A modern React meta-framework that human and ai could understand",
  "workspaces": [
    "packages/*",
    "examples/*",
    "docs",
    "playground"
  ],
  "scripts": {
    "build": "turbo --filter \"./packages/*\" build",
    "dev": "turbo --filter \"./packages/*\" dev",
    "test": "turbo --filter \"./packages/*\" test",
    "lint": "oxlint . && oxfmt --check .",
    "format": "oxlint --fix . && oxfmt .",
    "lint:fix": "oxlint --fix . && oxfmt .",
    "format:check": "oxfmt --check .",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "release:beta:farm": "turbo --filter \"./packages/*\" build && pnpm publish -r --filter \"./packages/*\" --access public --tag beta --no-git-checks",
    "version-packages": "changeset version",
    "release:beta": "turbo --filter \"./packages/*\" build && bumpp \"packages/*/package.json\" && pnpm -r --filter ./packages/* publish --access public --tag beta --no-git-checks",
    "release:no-build": "bumpp && pnpm -r publish --access public --no-git-checks --tag next",
    "release:canary": "turbo --filter \"./packages/*\" build && bumpp && pnpm -r publish --access public --tag canary --no-git-checks",
    "bump": "bumpp"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@types/node": "^20.10.5",
    "turbo": "^1.11.2",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@8.12.1",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "bumpp": "^10.3.1",
    "h3": "2.0.1-rc.5",
    "nitro": "3.0.0",
    "oxfmt": "^0.27.0",
    "oxlint": "^1.42.0"
  }
}