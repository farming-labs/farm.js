# Farm StyleX example

This example verifies the zero-config `@farm.js/stylex` development and production workflow.

```bash
pnpm --filter farm-stylex-demo dev
pnpm --filter farm-stylex-demo build
```
