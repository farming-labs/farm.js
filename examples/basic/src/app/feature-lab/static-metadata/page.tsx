export const metadata = {
  title: "Static metadata image",
  description: "A route using Farm's static Open Graph image convention.",
};

export default function StaticMetadataPage() {
  return <h1 data-testid="static-metadata-image">Static metadata image</h1>;
}
