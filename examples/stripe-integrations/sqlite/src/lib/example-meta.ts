export const exampleMeta = {
  title: "Stripe + Better Auth + SQLite",
  target: "SQLite",
  generateCommand: "pnpm --dir examples/stripe-integrations/sqlite generate",
  details:
    "Auto-detects SQLite and writes a raw SQL schema file you can inspect or apply yourself.",
} as const;
