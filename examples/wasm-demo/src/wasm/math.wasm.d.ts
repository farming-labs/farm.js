// The fixture has exactly these exports. Real libraries should supply their
// own generated declarations, for example via wasm-bindgen.
export function add(left: number, right: number): number;
export function divide(left: number, right: number): number;
