"use client";

// Keep the current browser runtime for navigation and page metadata while the
// production theme package supplies Farm's established Pixel Border CSS.
export { BrowserDocsLayout, BrowserRootProvider } from "@farming-labs/theme-runtime/browser";
